var mongoose = require('mongoose');

var lessonSchema = new mongoose.Schema({
    name: String,
    index: String,
    credit: Number,
    description: String,
    season: String
});


mongoose.model('Lesson', lessonSchema);

